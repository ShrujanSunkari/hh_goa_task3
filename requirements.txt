# ── Core Vision ──────────────────────────────────────────────────────────────
# tensorflow-cpu 2.15 supports Python 3.8-3.11 and requires numpy<2.
# The python_version marker ensures pip skips TF/DeepFace gracefully on
# Python 3.12+ instead of hard-failing.  The pipeline auto-falls-back to
# OpenCV Haar Cascade when DeepFace is absent (see face_detector.py).
#
# opencv-python is listed (same version) so deepface's transitive dep
# resolves to the headless wheels already installed, avoiding the Windows
# file-lock conflict between opencv-python and opencv-python-headless.
opencv-python-headless==5.0.0.93; python_version < "3.12"
opencv-python-headless>=4.10.0.84,<5.0.0; python_version >= "3.12"
opencv-python==4.9.0.80; python_version < "3.12"
tensorflow-cpu==2.15.0; python_version < "3.12"
deepface==0.0.93; python_version < "3.12"
numpy==1.26.4; python_version < "3.12"
numpy>=2.0.0; python_version >= "3.12"
fastapi==0.141.1
uvicorn==0.52.4
python-multipart==0.0.32

# ── Web / OSINT Search ───────────────────────────────────────────────────────
google-search-results==2.4.2
requests==2.34.2

# ── Blockchain ───────────────────────────────────────────────────────────────
web3==7.16.0
eth-tester==0.14.0b1
py-evm==0.12.1b1

# ── CLI / UX ─────────────────────────────────────────────────────────────────
rich==15.0.0

# ── Config / Utils ───────────────────────────────────────────────────────────
python-dotenv==1.2.3
Pillow==12.3.0
tenacity==9.1.4
cryptography==42.0.5

# ── Transitive Dependencies (Production) ─────────────────────────────────────
certifi==2026.7.22
charset-normalizer==3.5.1
idna==3.18
urllib3==2.7.0
pydantic==2.13.4
pydantic_core==2.46.4
typing_extensions==4.16.0
sniffio==1.3.1
anyio==4.14.2
starlette==1.6.0
h11==0.16.0
click==8.4.2
colorama==0.4.6
eth-abi==6.0.0
eth-account==0.14.0
eth-hash==0.8.0
eth-keyfile==0.10.0
eth-keys==0.8.0
eth-rlp==3.0.0
eth-typing==6.0.0
eth-utils==6.0.0
hexbytes==2.0.0
lru-dict==1.4.1
pycryptodome==3.23.0
rlp==5.0.0
websockets==15.0.1